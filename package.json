{
  "name": "kick-live-notifier",
  "version": "1.0.0",
  "description": "Sends a Telegram message when a Kick channel goes live, checked on a schedule via GitHub Actions.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  }
}
